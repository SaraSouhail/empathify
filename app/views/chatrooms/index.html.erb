<h1>Chatrooms</h1>
<ul class="list-group">
<li>
    <%= link_to @general.name, chatroom_path(@general), class: "list-group-item mb-3 chatroom-btn" %>
</li>
<li>
    <%= link_to @user_chatrooms.name, chatroom_path(@user_chatrooms), class: "list-group-item mb-3 chatroom-btn" %>
</li>
</ul>
